# Our binary is produced here
bin_PROGRAMS = passingarguments2
passingarguments2_SOURCES = passingarguments2.c
# main binary will be installed into the default directory for "bin"s (hence we used the bin prefix
