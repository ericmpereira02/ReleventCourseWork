# We don't want to follow GNU file setup (no README, no AUTHORS, etc)
AUTOMAKE_OPTIONS = foreign
# We have multiple dirs with code and (Makefile.am's) in them
SUBDIRS = creation_termination join passing_arguments1 passing_arguments2 
